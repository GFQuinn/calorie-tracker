<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weighttracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeightTracker">
        <activity android:name=".ActivityCreateMeal" />
        <activity android:name=".ActivityUserSettings" />
        <activity android:name=".ActivityAddFood" />
        <activity android:name=".ActivityGraph" />
        <activity android:name=".ActivityCalorieTrackerMenu" />
        <activity android:name=".ActivityWeightTracker" />

        <activity android:name=".ActivityAddItemPopup"
            android:theme="@style/PopupTheme"
            />
        <activity android:name=".ActivityMain"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>